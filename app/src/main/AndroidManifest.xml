<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifspsaocarlos.sdm.programacaoconcorrente">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">

        <activity
            android:name=".CarregandoActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PrincipalActivity"></activity>
        <activity android:name=".MensagemHandlerActivity"></activity>
        <activity android:name=".RunnableHandlerActivity" />
        <activity android:name=".RunOnUIThreadActivity" />


    </application>

</manifest>
